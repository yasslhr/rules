prefix=@CMAKE_INSTALL_PREFIX@
exec_prefix=${prefix}/bin
libdir=${prefix}/lib
sharedlibdir=${prefix}/lib
includedir=${prefix}/include

Name: rules
Description: Rules library

Requires:
Libs: -L${libdir} -L${sharedlibdir} -lrules
Cflags: -I${includedir}
